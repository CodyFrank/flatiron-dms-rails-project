<h3><%= @employee.name %> </h3>
  <%= @employee.job_title %>

<%= button_to "Delete", @employee, method: :delete, data: { confirm: "Really?" } %>


<!--<% @employee.repair_orders.all.each do |ro| %>
  <% if ro.finished == false %>
    <div class="ro-box">
      <%= ro.customer.name %>
    </div>
  <% end %>
<% end %> -->